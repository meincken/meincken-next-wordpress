import { CMS_NAME, CMS_URL } from "@/lib/constants";

export const Intro = () => {
  return (
    <header className="flex-col md:flex-row flex items-center md:justify-between">
      <h1 className="text-6xl md:text-8xl font-bold tracking-tighter leading-tight md:pr-8">
        Blog
      </h1>
    </header>
  );
}
